extends Node #This node might be useless

# Called when the node enters the scene tree for the first time.
func _ready():
	#damage = GameDAta.skill_data["Ice Spear"].Damage #Old way to get from gamedata.gd
#	Server.FetchSkillDamage("Ice Spear", get_instance_id()) #New way to get data from server instead
#	print(Server.FetchSkillDamage("Ice Spear", get_instance_id()))
#
#	Server.FetchQuizTitle("Quiz Title", get_instance_id())
#	print(Server.FetchQuizTitle("Quiz Title", get_instance_id()))
	pass

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
