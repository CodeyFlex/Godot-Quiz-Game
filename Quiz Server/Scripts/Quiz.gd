extends Node

func FetchQuiz(quiz_name): #not being used atm
	var quiz = ServerData.quiz_disney
	return quiz

func FetchBRQuiz(quiz_name): #not being used atm
	var quiz = ServerData.quiz_BR
	return quiz
